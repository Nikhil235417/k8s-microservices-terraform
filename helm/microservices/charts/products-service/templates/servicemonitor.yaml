apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: products-service
  namespace: dev
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: products-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s